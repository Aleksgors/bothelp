{
    "name": "my-applications/test-task",
    "license": "proprietary",
    "description": "Test task for BotHelp",
    "require": {
        "php-amqplib/php-amqplib": ">=3.0",
        "ramsey/uuid": "~3.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "mkdir -p data/logs && chmod 777 data/logs"
        ],
        "post-update-cmd": [
            "mkdir -p data/logs && chmod 777 data/logs"
        ]
    }
}
